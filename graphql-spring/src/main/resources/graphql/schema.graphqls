type Pessoa{
	cod: ID!
	nome: String,
	email: String,
	bairro: String,
	genero: String
}

type Pet {
	cod: ID!,
	nome: String,
	dono: Pessoa
}

type Query {
	findAllPeople: [Pessoa]
	findPeopleById(cod: Int): Pessoa
	
	findAllPets: [Pet]
	findPetById(cod: Int): Pet
}